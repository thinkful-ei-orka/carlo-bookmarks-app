const bookmarks = [];
const adding = false;
const error = null;
const filter = 0;


function addBookmark (item) {
    this.bookmarks.push(item);
}

function addExpanded (item) {
    
}




export default {
    bookmarks,
    adding,
    error,
    filter,
    addBookmark
}
